{% extends 'base.html.twig' %}

{% block title %}Edit Pessoas{% endblock %}


{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('new_app') }}
    {{ encore_entry_link_tags('edit_app') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('new_app') }}
    {{ encore_entry_script_tags('edit_app') }}
{% endblock %}


{% block body %}
    <h1>Edit Pessoas</h1>

    {{ include('pessoas/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_pessoas_index') }}">back to list</a>

    {% if app.user is not null %}
        {% if app.user.roles.0 == "ROLE_ADMIN" %}
            {{ include('pessoas/_delete_form.html.twig') }}
        {% endif %}
    {% endif %}

{% endblock %}
